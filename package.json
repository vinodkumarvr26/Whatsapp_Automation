{
  "name": "nexchat",
  "private": true,
  "version": "0.1.0",
  "description": "Production-grade WhatsApp automation platform monorepo scaffold",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  }
}
